{
  "cycle": 4,
  "stage": "phase-5 global counter (Pages Function + KV)",
  "gate": { "style_assertions": { "checks": 106, "failures": 0, "status": "PASS" }, "tests": { "total": 19, "pass": 19 }, "runtime_errors": 0 },
  "function_tests_via_curl": {
    "GET": "returns { houseWins, violations, matches, seasons, humanWins: 0 } seeded",
    "POST normal": "deltas applied and clamped; totals returned",
    "POST with humanWins/humanWinsDelta in body": "humanWins stays 0 — it is not a variable",
    "POST over-cap violationsDelta 99999": "clamped to 30",
    "POST malformed body / JSON array": "400",
    "rate limit": "one write per IP-hash per 60s (KV minimum TTL); throttled response returns current totals so the client never breaks",
    "KV persistence": "verified — totals survive a wrangler restart"
  },
  "client": {
    "offline": "static server (no function) -> GET 404 -> strip still shows THE HOUSE 52,140 - HUMANITY 0 from seed; game unaffected",
    "online": "verified end-to-end against `wrangler pages dev` — strip synced from GET, then incremented by 1 on match end (52,142 -> 52,143), KV persisted",
    "optimistic": "local figure bumps immediately on match end; a throttled POST no longer snaps it back"
  }
}
